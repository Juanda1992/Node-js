# Notes

Node program with Yargs custom command lines.
*View Markdown openpreview

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -n, --numero   Es la base numerica de la tabla de multiplicar
                                                             [number] [required]       
  -l, --listar   Muestra la tabla en consola
                                           [boolean] [required] [default: false]       
  -h, --hasta    Hasta que numero ira la tabla de multiplicar
                                               [number] [required] [default: 10]    
```